cd global/styles/
sass --watch scss:css

cd components/object-entries
sass --watch local.scss:local.css

===========================

git status
git add --all
git reset ../../00.txt
git commit --all -m "
git push

===========================
https://mdugg.github.io/js-funhouse/
