Next:
- image download states
- color palette separate project
- video api - stitch together videos / custom controls / access data, print to HTML
- google maps api (Irish Castles with info card / instagram photo ??)
- form validation, multi stage, regex (cheatsheet)
- web component modal
- JIRA drag and drop, on drop event re-number, store order in local storage

===========================

cd global/styles/
sass --watch scss:css

cd projects/search-wikipedia-api
sass --watch search-wikipedia.scss:search-wikipedia.css

===========================

git status
git add .
git commit -m "search wikipedia and hide the project tags for now"
git push

--
git add --all
git reset 00.txt
git rrm -- 00.txt
git fetch --all
git push --force

===========================
