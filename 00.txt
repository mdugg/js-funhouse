Next:
- about: prod design bio // what js-funhouse is about, sandbox for vanilla JS // s3 bucket for profile pics
- search app / fetch api (API rep)
- form validation, multi stage
- web component modal
- twitter api
- scroll animations / events
- screen scraping with JS
- accessibility - layer in
- JIRA drag and drop, on drop event re-number, store order in local storage

===========================

cd global/styles/
sass --watch scss:css

cd projects/intersectionObserver
sass --watch intersection-observer.scss:intersection-observer.css

===========================

git status
git add .
git commit -m "add the intersection observer project"
git push

--
git add --all
git reset 00.txt
git rrm -- 00.txt
git fetch --all
git push --force

===========================
