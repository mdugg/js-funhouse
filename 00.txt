Next:
- fix shopping cart
- array methods
- DOM manipulation
- fetch api
- form validation
- web component modal
- about: tw / ln / prod design bio
- lazy load images for cart
    https://www.sitepoint.com/five-techniques-lazy-load-images-website-performance/
- function factories / higher order functions / classes

===========================

cd global/styles/
sass --watch scss:css

cd components/shopping-cart
sass --watch local.scss:local.css

cd components/array-methods
sass --watch array-methods.scss:array-methods.css


===========================

git status
git add .
git commit -m "refactoring and latest 4 projects"
git push

--
git add --all
git reset 00.txt
git rrm -- 00.txt
git fetch --all
git push --force

===========================
